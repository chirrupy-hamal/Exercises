1、A ACSII65 a ASCII97 '0' ASCII48

2、按位与运算符&优先级高于加法运算符+

3、若使用数组作为函数参数，则以下说法正确的是？
   A、必须在主调函数中说明此数组的大小
   B、实参数组类型与形参数组类型可以不匹配
   C、在被调用函数中，不需要考虑形参数组的大小
   D、实参数组名与形参数组名必须一致
   选C。
   A选项：在传递一个带有'\0'的字符数组时是不需要的。
   B选项：牛客网上没有给出B选项是正确的，个人认为答案有误。
          例如，实参数组类型可以是char [10]，对应的形参数组可以是const char []。
                在VS08编译环境下，.c和.cpp均没问题。

4、请阅读下面代码片段并且回答问题。
   #define SIZE_20M (20*1024*1024)
   void func_a()
   {
       char *temp = malloc(SIZE_20M);
       return;
   }
   void func_b()
   {
       char temp[SIZE_20M];
       //… do something using temp
       return;
   } 
   关于这段代码，下列说法正确的是？
   A、func_a获得临时内存的方式效率通常更高
   B、func_b使用了太多的栈，程序可能会在运行时崩溃
   C、func_b存在内存泄漏
   D、func_a和func_b分配的内存会自动初始化成0
   选B。
   A选项：反了。
   D选项：calloc()函数分配的内存才会自动初始化成0。
