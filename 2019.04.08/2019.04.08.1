若有以下程序

#include <stdio.h>

int f(int a［ ］,int n) 
{ 
  if(n > 1) 
  { 
    int t; 
    t=f(a, n-1); 
    return t > a[ n -1 ] ？ t：a［n-1］;      
    } 
    else 
      return a[0];  
}

main() 
{  
  int a［ ］= {8,2,9,1,3,6,4,7,5};   
  printf("%d\n", f(a, 9)); 
}

则程序的输出结果是? 9

解析：本题实际上就是求出数组a中元素的最大值。

