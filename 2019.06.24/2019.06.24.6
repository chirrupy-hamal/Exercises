1、#define S345 //这样写是可以的，称为空宏定义。
   函数在声明的时候一定要写形参类型，而且在C语言中，{}必须作为函数体来使用。

2、下面C程序的输出结果：
   int i=,a=1,b=2,c=3;
   i=++a||++b||++c;
   printf("%d %d %d %d",i,a,b,c);
   答：1 2 2 3
   因为++a为"真"，所以++b和++c是不用执行的。&&也是如此。

3、int a[10]={};//C是不允许数组这样定义的，但C++可以，元素全部初始化为0。
   int a[10]={10,0,-1.0};//在VS08编译环境下，.c和.cpp均发出警告： 
   warning C4244: “初始化”: 从“double”转换到“int”，可能丢失数据
   而且牛客网上并没有选择这个答案，保险起见，还是不要选了。
