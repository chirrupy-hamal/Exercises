1、下面程序的运行结果是：
   #define N 2
   #define M N+1
   #define NUM (M+1)*M/2
   main()
   {
       printf("%d\n",NUM);
   }
   答：8，(2+1+1)*2+1/2=8。细心点!!!

2、设有char *s1="ABCDE",*s2="ABCDE",*s3=s1;下面表达式中值等于true的是？
   A、strcmp(s1,s2)  B、strcmp(s1,s3)==1  C、strcmp(s2,s3)==-1  D、strcmp(s1,s3)==0
   选D。
   设这两个字符串为str1，str2，若str1==str2，则返回0；若<，则返回负数；否则返回正数。

3、当顺利执行了文件关闭操作时，fclose函数的返回值是0。
   如果正常关闭了文件，则函数的返回值为0；否则返回非0。
   strcat()字符串连接函数；
   strcpy()和strncpy()，字符串复制函数，strcpy()连'\0'也一并复制过去。
   strlen()，测字符串长度的函数，其中不包含'\0'；
   strlwr()，转换为小写的函数；
   strupr()，转换为大写的函数；
   puts()，输出字符串的函数，该函数等价于printf("%s\n", );
   gets()，输入字符串的函数，其中空格并不会被编译系统认为是字符串之间的分隔符，而且'\0'也被一并输入。

4、转义字符 
   \?  \'  \"  \\  \n  \t  \r  \b  \a  \v  \f  \o \oo \ooo  \xh[h……]
   不合法的转义字符，此题的问答。 
   \1011(超出3位八进制数)  \  \ab(缺少x)  \}(这时合法的)  \xlf(不合法，十六进制数字中没有l) 
